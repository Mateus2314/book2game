// MSW Mock Server placeholder
// Handlers will be defined when needed for component integration tests

// Mock handlers - empty for now
export const handlers: any[] = [];

// Mock server - placeholder for future MSW integration
export const server = {
  listen: jest.fn(),
  resetHandlers: jest.fn(),
  close: jest.fn(),
} as any;